<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/titleBgColor</item>
        <item name="colorPrimaryDark">@color/titleBgColor</item>
        <item name="colorAccent">@color/titleBgColor</item>
    </style>

</resources>
